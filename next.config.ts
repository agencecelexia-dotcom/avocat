import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static HTML files in public/ serve the main site
  // Admin dashboard is handled by Next.js App Router
};

export default nextConfig;
